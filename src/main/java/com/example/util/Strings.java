package com.example.util;

public class Strings {
  /**
   * Add quote to the given string.
   *
   * @param token input token
   * @return token with quotation
   */
  public static String quote(String token) {
    return "\"" + token + "\"";
  }
}
